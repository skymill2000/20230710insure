function plus(p1, p2) {
  console.log(p1 + p2);
}

plus(3, 9);

const minus = (p1, p2) => {
  console.log(p1 - p2);
};

minus(3, 9);

const mul = (p1, p2) => {
  console.log(p1 * p2);
};

mul(3, 9);

const div = (p1, p2) => {
  console.log(p1 / p2);
};

div(3, 9);
